// =============================================================
// src/secrets.h.template
// =============================================================
// INSTRUCCIONES:
//   1. Copia este fichero y renombralo a: src/secrets.h
//   2. Rellena tus valores reales.
//   3. NO subas secrets.h a git (ya esta en .gitignore).
//
// PROPOSITO:
//   Define los valores por defecto que se cargan la PRIMERA VEZ
//   que el firmware arranca en un dispositivo virgen (NVS vacia).
//   A partir del primer guardado desde la web (/config o modo Setup),
//   los valores quedan en la NVS (flash) del ESP32 y estas
//   macros dejan de tener efecto.
// =============================================================

#pragma once

// --- WiFi (solo se usa si la conexion seleccionada es WiFi) ---
#define DEFAULT_WIFI_SSID      "TU_SSID_WIFI"
#define DEFAULT_WIFI_PASSWORD  "TU_PASSWORD_WIFI"

// --- Destino Modbus TCP (EMMA / inversor Huawei) ---
#define DEFAULT_MODBUS_IP      "192.168.1.100"   // IP de tu EMMA o inversor
#define DEFAULT_MODBUS_PORT    502

// --- Red estatica del proxy (solo cuando useDHCP = false) ---
#define DEFAULT_LOCAL_IP       "192.168.1.103"   // IP que tendra el proxy en tu red
#define DEFAULT_GATEWAY        "192.168.1.1"     // Puerta de enlace de tu router
#define DEFAULT_SUBNET         "255.255.255.0"
#define DEFAULT_DNS            "192.168.1.1"

// --- Contrasena OTA (debe coincidir con el valor en secrets.ini) ---
#define DEFAULT_OTA_PASSWORD   "tu_password_ota"
